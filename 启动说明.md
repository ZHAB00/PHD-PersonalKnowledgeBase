# PDH-PKG 个人知识库系统 — 启动说明

## 第一次启动（快速开始）

1. 进入项目目录并激活 Python 环境（如有 conda/venv）：
   ```powershell
   cd "<项目根目录>"
   conda activate <你的环境名>
   ```
2. 安装依赖并创建 `.env`：
   ```powershell
   python -m pip install -r requirements.txt
   Copy-Item .env.example .env
   ```
3. 编辑 `.env`，填入 `DEEPSEEK_API_KEY`（使用云端对话时必填）：
   ```powershell
   notepad .env
   ```
4. 可选服务：Redis（缓存加速）、Ollama（本地模型/向量）；如需要知识图谱，再启动 Neo4j。默认向量模型是 Ollama `qwen3-embedding:4b`，如不用 Ollama 可在设置页切换为本地 ONNX。
5. 首次启动建议使用调试模式：
   ```powershell
   $env:PDH_PKG_DEBUG='1'
   python -m uvicorn app.main:app --host 127.0.0.1 --port 8001 --reload
   ```
6. 浏览器打开 **http://127.0.0.1:8001**，首次访问会自动获取本地令牌，不需要登录。
7. 在“设置”页配置对话模型、向量模型、联网搜索和 Neo4j，并点击对应的“测试连接”确认可用。
8. 上传需要入库的文档，等状态变为“就绪”后即可开始提问。

> 调试模式与正式模式配置隔离，详见下文“调试模式”。

## 环境信息

| 项 | 值 |
|---|---|
| 项目路径 | <项目根目录> |
| Python 环境 | 任意 Python 3.10+（可使用 conda/venv） |
| Python 路径 | python（可通过 `PDH_PKG_PYTHON` 指定） |

## 依赖服务 (按启动顺序)

### 1. Qdrant（向量库，必需）
- **端口**: 6333
- **用途**: 文档向量存储与检索
- **安装**: 从 Qdrant Release 下载后放到 `qdrant\qdrant.exe`；后端启动时会自动拉起
- **验证**: `Test-NetConnection 127.0.0.1 -Port 6333`

### 2. Redis（可选）
- **端口**: 6379
- **用途**: 对话缓存、记忆缓存
- **验证**: `redis-cli PING` 返回 `PONG`
- **不启动也能用**: 自动回退到进程内内存

### 3. Ollama（可选，本地模型）
- **启动**: `ollama serve`
- **端口**: 11434
- **模型**: `qwen3-embedding:4b`
- **用途**: 本地聊天模型、Ollama 向量模型
- **验证**: `ollama list` 能看到对应模型
- **不启动也能用**: 默认向量模型是内置本地 ONNX，不依赖 Ollama

### 4. Neo4j (知识图谱, 可选)
- **手动启动**:
  ```
  <Neo4j安装目录>\bin\neo4j.bat console
  ```
- **端口**: bolt://localhost:7687, http://localhost:7474
- **用户名/密码**: 按你本机 Neo4j 实际安装时的账号配置
- **用途**: 实体关系图谱存储, 构建和检索知识图谱
- **不启动也能用**: 向量检索和对话功能正常, 仅图谱功能不可用

### 5. Tesseract OCR（可选，源码调试用）
- **安装包**: 已内置 Tesseract OCR 与中文/英文语言包，无需手动安装
- **源码调试**: 安装 Tesseract 并加入 PATH，或在 `.env` 中通过 `TESSERACT_CMD` 指定路径
- **验证**: 设置页显示“OCR: 可用”
- **不配置也能用**: 普通文档解析正常，仅扫描版 PDF 会跳过文字识别

## 启动后端服务

```powershell
# 1. 激活你的 Python 环境（如有 conda/venv）
conda activate <你的环境名>

# 2. 进入项目目录
cd "<项目根目录>"

# 3. 启动 (生产模式)
python -m uvicorn app.main:app --host 127.0.0.1 --port 8001

# 或带热重载 (开发模式)
python -m uvicorn app.main:app --host 127.0.0.1 --port 8001 --reload
```

### 启动成功标志
```
INFO:app.main:LangChain 1.0+ tools loaded: 6 tools
INFO:app.main:PDH-PKG 服务启动 - 端口 8001
INFO:app.main:Qdrant 已启动（端口 6333）
INFO:app.core.cache:Redis 连接就绪
INFO:app.main:Qdrant collections 就绪
INFO:app.rag.graph_rag:Neo4j connected: bolt://localhost:7687   <-- 如果 Neo4j 启动了
INFO:     Application startup complete.
```

## 联网搜索（无需 API Key）

系统内置 `web_search` 工具，默认 `auto` 模式不需要任何配置：

- 未配置任何 Key：优先使用 Bing，失败后自动切换 DuckDuckGo
- 配置了 Tavily Key：自动优先使用 Tavily
- 配置了 SearXNG Base URL：自动使用 SearXNG

在设置页“联网搜索”中可修改提供商、Base URL、API Key，也可以点击“测试连接”验证。API Key 保存后只显示为 `***`，不会回显明文。

## 调试模式（推荐日常开发用）

调试模式不读取 `settings.json` / `settings.debug.json`，固定使用内置调试默认值：

- 对话模型：DeepSeek（读取 `.env` 的 `DEEPSEEK_*`，默认 `deepseek-v4-flash`）
- 向量模型：调试模式使用 Ollama `qwen3-embedding:4b`；正式模式默认内置本地模型 `BAAI/bge-small-zh-v1.5`
- 知识图谱：Neo4j 本地默认 `bolt://localhost:7687`（账号按 `.env` 或设置页配置）
- 设置页在调试模式下保存不会影响运行
- 正式模式只读 `settings.json`，永远不会读取调试值

### 手动启动

```powershell
cd "<项目根目录>"
$env:PDH_PKG_DEBUG='1'
python -m uvicorn app.main:app --host 127.0.0.1 --port 8001 --reload
```

### 配置隔离说明

- 调试模式：固定使用内置调试默认值（DeepSeek + Ollama + 本地 Neo4j），读取 `.env` 但不会读 `settings.json` / `settings.debug.json`。
- 正式模式：只读 `settings.json`，不读任何调试值。
- 需要调试自定义模型时，先关掉 `PDH_PKG_DEBUG` 再通过设置页配置。

## 端口一览

| 服务 | 端口 | 说明 |
|---|---|---|
| PDH-PKG 后端 | 8001 | 主要访问入口 |
| Qdrant (向量库) | 6333 | 后端自动拉起 |
| Redis | 6379 | 可选，未启动时内存回退 |
| Ollama | 11434 | 可选，默认向量模型为 Ollama `qwen3-embedding:4b` |
| Neo4j Bolt | 7687 | 需手动启动 |
| Neo4j HTTP | 7474 | 图形化管理界面 |

## 前端访问

浏览器打开: **http://127.0.0.1:8001**

## 配置项

配置文件: `.env`, `app/config.py`

| 配置 | 说明 |
|---|---|
| `DEEPSEEK_MODEL` | 对话模型 (`deepseek-v4-flash`, thinking 模式) |
| `DEEPSEEK_API_KEY` | DeepSeek API Key (在 `.env` 中) |
| `EMBEDDING_MODEL` | Ollama 向量模型；内置本地模型在设置页选择（默认 `BAAI/bge-small-zh-v1.5`） |
| `NEO4J_PASSWORD` | Neo4j 密码（在 `.env` 或设置页配置） |
| `NEO4J_BIN` | Neo4j 启动脚本路径（留空则不自动拉起） |
| `NEO4J_JAVA_HOME` | Java 21 安装目录（Neo4j 自动拉起时使用） |
| `REDIS_URL` | Redis 连接 (`redis://127.0.0.1:6379/0`) |
| `SERVICE_PORT` | 后端端口 (`8001`) |
| `SEARCH_*` | 联网搜索配置（provider / api_key / base_url），在设置页保存，不读取 `.env` |
| `TESSERACT_CMD` | OCR 可执行文件；打包版自动使用内置目录，源码调试默认 `tesseract` |
| `OCR_LANG` | OCR 语言，默认 `chi_sim+eng` |

## 用户账号

| 用户名 | 密码 |
|---|---|
| admin | admin123（开发默认） |
| user | user123（开发默认） |

个人本地使用无需登录：前端会自动从 `/api/auth/local-token` 获取本地令牌。上面的预设账号仅用于开发或需要显式调用 `/api/auth/login` 的场景；生产环境请在 `.env` 中通过 `PRESET_USERS` 修改。如果开启了设置页的“访问密码”，则使用本地密码登录。

## 单实例说明

PDH-PKG 是单实例应用。重复运行启动程序不会创建第二个窗口，而是将已运行的窗口显示到前台。

## 关闭服务

1. 后端: `Ctrl+C`
2. Ollama: 关闭终端窗口或 `taskkill /IM ollama.exe /F`
3. Neo4j: `Ctrl+C`
4. Redis: `redis-cli SHUTDOWN`

## 常见问题

### 启动后端报 "port 8001 already in use"
```powershell
# 查看占用端口的进程
netstat -ano | findstr :8001
# 终止进程 (替换 PID)
taskkill /PID <PID> /F
```

### Ollama 报 "bind: Only one usage"
说明 Ollama 已经在运行, 不需要再启动。

### Neo4j 连不上
1. 确认 Neo4j 已启动 (看终端有无 `Started` 日志)
2. 确认密码与 Neo4j 实际配置一致
3. 首次使用可能需要改密码: 访问 http://localhost:7474

### 乱码显示
PowerShell 终端编码问题, 不影响功能。执行 `chcp 65001` 可临时修复。

### Redis 连接失败
确保 Redis 服务已启动。Windows 上如果是 Redis-x64 绿色版, 在 Redis 目录下运行 `redis-server.exe`。

### 扫描版 PDF 提示“未检测到 Tesseract OCR”
安装包已内置 Tesseract，正常安装后无需处理。源码方式启动时需自行安装 Tesseract，或通过 `.env` 的 `TESSERACT_CMD` 指定可执行文件路径。

### 启动报 "No module named 'app'"
说明当前目录不是项目根目录。先 `cd "<项目根目录>"` 再启动。

### 切换调试/正式模式后一直 401
浏览器里残留了旧的登录 token。刷新页面即可，前端会自动清理失效 token 并重新获取本地 token。

### 检索工具报 "_multi_query_retrieve() missing..."
这是旧版后端与新版前端缓存不匹配导致的调用参数错误，已修复。刷新页面并重启后端即可；如仍出现，先清浏览器缓存或强制刷新（`Ctrl+F5`）。
