# 企业知识库系统 — 启动说明

## 环境信息

| 项 | 值 |
|---|---|
| 项目路径 | E:\aProgramming_code\GetAJobProject\企业知识库搭建 |
| Conda 环境 | enterprise_kb |
| Python 路径 | E:\anaconda3\envs\enterprise_kb\python.exe |

## 依赖服务 (按启动顺序)

### 1. Redis
- **端口**: 6379
- **用途**: 对话缓存、记忆缓存
- **验证**: `redis-cli PING` 返回 `PONG`

### 2. Ollama (本地 Embedding)
- **启动**: `ollama serve`
- **端口**: 11434
- **模型**: `qwen3-embedding:4b`
- **用途**: 文档向量化和检索时的 query 向量化
- **验证**: `ollama list` 能看到 `qwen3-embedding:4b`

### 3. Neo4j (知识图谱, 可选)
- **手动启动**:
  ```
  E:\neo4j-chs-community-2026.05.0-windows\bin\neo4j.bat console
  ```
- **端口**: bolt://localhost:7687, http://localhost:7474
- **用户名/密码**: `neo4j` / `kb123456`
- **用途**: 实体关系图谱存储, 构建和检索知识图谱
- **不启动也能用**: 向量检索和对话功能正常, 仅图谱功能不可用

## 启动后端服务

```powershell
# 1. 激活 conda 环境
conda activate enterprise_kb

# 2. 进入项目目录
cd "E:\aProgramming_code\GetAJobProject\企业知识库搭建"

# 3. 启动 (生产模式)
E:\anaconda3\envs\enterprise_kb\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8001

# 或带热重载 (开发模式)
E:\anaconda3\envs\enterprise_kb\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8001 --reload
```

### 启动成功标志
```
INFO:app.main:LangChain 1.0+ tools loaded: 5 tools
INFO:app.main:企业知识库服务启动 - 端口 8001
INFO:app.main:Qdrant 已启动（端口 6333）
INFO:app.core.cache:Redis 连接就绪
INFO:app.main:Qdrant collections 就绪
INFO:app.rag.graph_rag:Neo4j connected: bolt://localhost:7687   <-- 如果 Neo4j 启动了
INFO:     Application startup complete.
```

## 端口一览

| 服务 | 端口 | 说明 |
|---|---|---|
| 企业知识库后端 | 8001 | 主要访问入口 |
| Qdrant (向量库) | 6333 | 后端自动拉起 |
| Redis | 6379 | 需先启动 |
| Ollama | 11434 | 需先启动 `ollama serve` |
| Neo4j Bolt | 7687 | 需手动启动 |
| Neo4j HTTP | 7474 | 图形化管理界面 |

## 前端访问

浏览器打开: **http://127.0.0.1:8001**

## 配置项

配置文件: `.env`, `app/config.py`

| 配置 | 说明 |
|---|---|
| `DEEPSEEK_MODEL` | 对话模型 (`deepseek-v4-flash`, thinking 模式) |
| `DEEPSEEK_API_KEY` | DeepSeek API Key (在 `.env` 中) |
| `EMBEDDING_MODEL` | 本地向量模型 (`qwen3-embedding:4b`) |
| `NEO4J_PASSWORD` | Neo4j 密码 (`kb123456`) |
| `REDIS_URL` | Redis 连接 (`redis://127.0.0.1:6379/0`) |
| `SERVICE_PORT` | 后端端口 (`8001`) |

## 用户账号

| 用户名 | 密码 | 角色 |
|---|---|---|
| admin | admin123 | 管理员 |
| user | user123 | 普通用户 |

## 关闭服务

1. 后端: `Ctrl+C`
2. Ollama: 关闭终端窗口或 `taskkill /IM ollama.exe /F`
3. Neo4j: `Ctrl+C`
4. Redis: `redis-cli SHUTDOWN`

## 常见问题

### 启动后端报 "port 8001 already in use"
```powershell
# 查看占用端口的进程
netstat -ano | findstr :8001
# 终止进程 (替换 PID)
taskkill /PID <PID> /F
```

### Ollama 报 "bind: Only one usage"
说明 Ollama 已经在运行, 不需要再启动。

### Neo4j 连不上
1. 确认 Neo4j 已启动 (看终端有无 `Started` 日志)
2. 确认密码是 `kb123456`
3. 首次使用可能需要改密码: 访问 http://localhost:7474

### 乱码显示
PowerShell 终端编码问题, 不影响功能。执行 `chcp 65001` 可临时修复。

### Redis 连接失败
确保 Redis 服务已启动。Windows 上如果是 Redis-x64 绿色版, 在 Redis 目录下运行 `redis-server.exe`。
