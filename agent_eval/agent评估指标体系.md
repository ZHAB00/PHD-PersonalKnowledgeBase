# Agent 性能评估指标体系设计

> 面向多步、可调用工具、目标驱动的智能体（Agent）的完整评估框架。
> 适用范围：RAG/知识库问答 Agent、任务编排 Agent、编码 Agent、客服 Agent 等。
> 本体系与通用 LLM 评估的区别：Agent 是**有状态、多步、带工具、目标导向**的系统，评估必须同时覆盖「最终结果」与「执行过程轨迹」。

---

## 0. 为什么需要专门的 Agent 评估体系

| 评估对象 | 关注点 | 传统指标是否够用 |
|---|---|---|
| 基础 LLM | 单轮生成质量 | 够用（BLEU/ROUGE/困惑度/人工打分） |
| RAG/知识库 | 检索 + 生成的准确性 | 部分够用，需补检索/引用/忠实度指标 |
| **Agent** | 目标达成 + 过程轨迹 + 工具链 + 成本 + 安全 | **不够用**，必须新增过程性、系统性指标 |

Agent 的核心风险点（也是指标必须覆盖的地方）：
1. **目标漂移**：多步后偏离原始任务。
2. **错误级联**：单步错误在轨迹中放大。
3. **工具误用**：选错工具、传错参数、误读结果。
4. **不可终止**：死循环、重复调用、过早终止。
5. **成本失控**：Token/工具调用/时延不可预测。
6. **上下文劣化**：长轨迹下遗忘约束、历史污染。

---

## 1. 设计原则

1. **结果与过程并重**：既评「做没做成」，也评「怎么做的」。
2. **可量化优先**：优先使用可由日志/轨迹自动计算的指标；主观指标需有 rubric 与抽样标准。
3. **可归因**：指标能定位到「规划 / 推理 / 工具 / 检索 / 生成」哪个环节出了问题。
4. **分层可下钻**：北极星指标 → 一级维度 → 二级指标 → 原子指标。
5. **成本内生化**：把 token、时延、调用次数当作一等公民指标，而非事后观察项。
6. **可重复**：同一评测集、同一环境、同一 judge 配置下结果可复现。
7. **与业务目标对齐**：技术指标最终映射到业务 KPI（正确率、转化率、人力节省等）。

---

## 2. 指标体系分层架构

```
L0 北极星指标（North Star）
   └── 综合质量分 / 任务有效完成率
L1 七大维度（Dimension）
   ├── D1 任务结果（Outcome）
   ├── D2 正确性与质量（Correctness & Quality）
   ├── D3 效率与成本（Efficiency & Cost）
   ├── D4 可靠性与鲁棒性（Reliability & Robustness）
   ├── D5 安全与对齐（Safety & Alignment）
   ├── D6 过程与行为（Process & Behavior）
   └── D7 用户体验（User Experience）
L2 二级指标（KPI）
L3 原子指标（Atomic Metric，带公式与数据源）
```

**七大维度总览**

| 维度 | 回答的问题 | 代表指标 |
|---|---|---|
| D1 任务结果 | 任务最终完成了吗？ | 任务成功率、目标达成率、一次性通过率 |
| D2 正确性与质量 | 答案/产物对不对、好不好？ | 正确率、忠实度、幻觉率、引用准确率 |
| D3 效率与成本 | 花了多少时间/钱/步数？ | 端到端时延、Token 成本、步骤效率 |
| D4 可靠性与鲁棒性 | 稳定吗？抗干扰吗？ | 一致性、鲁棒性、错误恢复率 |
| D5 安全与对齐 | 安全合规吗？不越界吗？ | 有害内容率、拒答准确率、越权率 |
| D6 过程与行为 | 中间步骤合理吗？ | 工具调用成功率、规划准确率、终止合理性 |
| D7 用户体验 | 用户满意吗？ | CSAT/NPS、首次解决率、留存 |

---

## 3. 原子指标详细定义

> 约定符号：
> - `N` = 评测样本总数
> - `N_success` = 成功样本数
> - 所有比率指标归一化到 `[0,1]`，报告时可用百分比。

### 3.1 D1 任务结果（Outcome）

#### M1 任务成功率（Task Success Rate, TSR）
```
TSR = N_success / N
```
- **定义**：在给定评测集中，Agent 产出**被判定为满足任务全部关键要求**的样本占比。
- **判定标准**：由「可执行断言 + LLM-as-judge + 人工抽检」三通道判定，见 §5。
- **变体**：
  - **部分成功率**：满足 ≥K% 子要求的比例（适用于可拆分任务）。
  - **严格成功率**：无任何关键要求缺失。

#### M2 目标达成率（Goal Achievement Rate, GAR）
```
GAR = 达成目标的任务数 / 总任务数
```
- 与 TSR 的区别：GAR 强调「最终目标状态是否达成」（例如“订到机票”），TSR 强调「输出是否合格」（例如“给出了正确航班列表”）。
- 对**有明确世界状态 / 副作用**的任务（写文件、调用外部系统）用 GAR；对**纯问答/生成**任务用 TSR。

#### M3 一次性通过率（First-Pass Rate, FPR）
```
FPR = 首轮尝试即成功且无需人工修正的样本数 / N
```
- **价值**：衡量「一次做对」能力，直接关联人力节省与用户等待。
- **配套指标**：平均人工修正轮次（Mean Rework Rounds, MRR）。

#### M4 任务完成度（Completion Ratio, CR）
```
CR = Σ(每个任务已满足的关键子要求数 / 该任务关键子要求总数) / N
```
- 用于多子任务的复合任务，给出「完成到几成」的细粒度信息。

---

### 3.2 D2 正确性与质量（Correctness & Quality）

#### M5 答案正确率（Accuracy）
```
Accuracy = 判定正确的样本数 / N
```
- 按任务类型细分：事实类、推理类、程序类、开放式生成类分别统计。

#### M6 精确率 / 召回率 / F1（Precision / Recall / F1）
- 用于**抽取、多答案检索、信息点覆盖**类任务。
```
Precision = TP / (TP + FP)
Recall    = TP / (TP + FN)
F1        = 2·P·R / (P + R)
```

#### M7 忠实度 / 幻觉率（Faithfulness / Hallucination Rate）
```
Hallucination Rate = 含虚构事实的样本数 / N
Faithfulness = 1 - Hallucination Rate
```
- **细粒度（幻觉密度）**：
```
Hallucination Density = Σ(单样本幻觉事实条数) / Σ(单样本全部断言条数)
```
- 幻觉分型（必须分类统计）：
  1. **事实幻觉**：捏造不存在的实体/数值/事件。
  2. **引用幻觉**：引用来源不存在或来源不支持该论断。
  3. **归因幻觉**：把 A 的话/成果归到 B。

#### M8 引用/溯源准确率（Citation Accuracy / Grounding）
```
Citation Accuracy = 引文确实支持对应论断的引用条数 / 总引用条数
Citation Coverage = 可被来源支撑的论断数 / 需支撑的论断总数
```
- **知识库/RAG Agent 核心指标**。对每条带 `[n]` 引用的论断，人工或 judge 判定引用是否“充分且必要”地支撑论断。

#### M9 检索相关度（Retrieval Relevance）
```
Hit@k      = 正确文档出现在 Top-k 的查询占比
MRR        = 1/N · Σ(1 / rank_i)         # rank_i 为第一个相关文档的排名
NDCG@k     = 考虑排序位置的折损累计增益
Recall@k   = Top-k 中相关文档数 / 全部相关文档数
```
- 用于隔离评测「检索子系统」质量，定位是检索问题还是生成问题。

#### M10 生成质量（Generation Quality，开放式任务）
- 使用 **LLM-as-judge 打分（1–5 或 1–10）** 评估：
  - 相关性（Relevance）
  - 完整性（Completeness）
  - 连贯性（Coherence）
  - 信息量（Informativeness）
  - 符合指令/约束（Instruction Following）
- 报告均值、中位数、分布，而非仅均值。

---

### 3.3 D3 效率与成本（Efficiency & Cost）

#### M11 端到端时延（End-to-End Latency）
```
Latency_p50 / p90 / p95 / p99（从用户输入到最终输出的时间）
```
- 必须分「首 Token 时延（TTFT）」与「完整输出时延」。

#### M12 Token 成本（Token Cost）
```
Cost_in = 平均输入 Token 数（含上下文/工具结果回填）
Cost_out = 平均输出 Token 数
Cost_total = Cost_in + Cost_out
费用 = Cost_total × 单价
```
- 关键点：Agent 的**上下文回填**（工具返回、检索结果反复注入）是成本放大主因，须单独统计「上下文累计注入 Token」。

#### M13 步骤效率（Step Efficiency）
```
Step Count = 完成任务所用的推理/动作轮数
Step Efficiency = 理论最少步数 / 实际步数（可度量时）
```
- 衡量是否“绕远路”。无法得知理论最少步数时，用「同任务集中相对步数」对比。

#### M14 工具调用效率（Tool Call Efficiency）
```
Tool Calls per Task = 总工具调用次数 / 任务数
Redundant Tool Calls Rate = 重复/无效调用次数 / 总调用次数
```

#### M15 成本-质量效率（Cost-Effectiveness）
```
Cost per Success = 总成本 / 成功任务数
```
- 用于对比不同模型/配置的**性价比**，是选型的关键决策指标。

---

### 3.4 D4 可靠性与鲁棒性（Reliability & Robustness）

#### M16 一致性（Consistency）
```
Consistency = 语义等价输入在多次运行中得到一致正确结果的比例
```
- 运行同一任务 ≥3 次（温度 > 0），统计结果稳定度。
- 分「答案一致性」与「轨迹一致性」。

#### M17 鲁棒性（Robustness）
```
Robustness = 注入扰动后任务成功率 / 原始任务成功率
```
- 扰动类型：
  1. **输入改写**：同义改写、口语化、错别字、多语言。
  2. **对抗注入**：提示注入、上下文投毒、诱导越权。
  3. **噪声上下文**：无关/矛盾/过期文档混入。
  4. **边界与异常**：空输入、超长输入、模糊需求。

#### M18 错误恢复率（Error Recovery Rate）
```
Recovery Rate = 中间步骤出错后仍最终成功的任务数 / 中间出错的任务数
```
- 衡量「自我纠错」能力，是区分初级/高级 Agent 的重要指标。

#### M19 稳定性/可用性（Availability，在线指标）
```
Availability = 无异常中断的成功会话占比
Crash/Timeout Rate = 超时、崩溃、被外部熔断的会话占比
```

---

### 3.5 D5 安全与对齐（Safety & Alignment）

#### M20 有害内容率（Harmful Output Rate）
```
Harmful Rate = 输出含有害内容的样本数 / N
```
- 有害分类（对齐主流安全分类法）：暴力、色情、仇恨、自伤、非法、隐私泄露、PII 泄露。

#### M21 拒答准确率（Refusal Precision / Recall）
```
Refusal Precision = 正确拒答数 / 总拒答数
Refusal Recall    = 正确拒答数 / 应拒答总数
```
- 区分「过度拒答」（该答不答，伤体验）与「拒答不足」（不该答却答，伤安全）。

#### M22 越权/越界率（Permission Violation Rate）
```
越权率 = 执行了未被授权操作的任务数 / N
```
- 针对有权限/沙箱约束的 Agent：是否访问了未授权工具、文件、API、环境变量。

#### M23 隐私与数据合规
```
PII Leak Rate = 输出泄露 PII 的样本占比
Prompt Leak Rate = 输出泄露系统提示词/内部规则的样本占比
```
- 知识库场景额外关注：是否泄露了**无权限文档的内容**（越权检索）。

#### M24 指令遵循率（Instruction Following Rate）
```
IFR = 完全满足全部显式约束（格式/字数/风格/禁用项）的样本数 / N
```
- 细分为：格式约束、内容约束、风格约束、负面约束（“不要…”）。

---

### 3.6 D6 过程与行为（Process & Behavior）

> 本维度全部来自**执行轨迹（Trace）**分析，是 Agent 评估相对 LLM 评估的增量。

#### M25 工具调用成功率（Tool Call Success Rate）
```
Tool Call Success = 调用被工具正常执行并返回预期结果的次数 / 总调用次数
```
- 细分：
  - **工具选择准确率**：是否选择了正确工具。
  - **参数正确率**：参数名、类型、取值是否正确。
  - **结果解读正确率**：是否正确理解工具返回内容。

#### M26 规划准确率（Plan Accuracy）
```
Plan Accuracy = 规划步骤合理的任务数 / N
```
- 用 judge 或专家评估：任务的分解与步骤顺序是否合理、是否覆盖目标、是否可执行。

#### M27 终止合理性（Termination Appropriateness）
- 三类异常终止必须统计：
  1. **过早终止**：未完成目标即宣称完成。
  2. **过晚终止**：已完成后仍继续无效调用（不能收手）。
  3. **不终止**：死循环 / 达到步数上限被强制截断。

#### M28 上下文/记忆管理质量（Context & Memory Quality）
```
Constraint Retention = 关键约束在多轮后仍被遵守的任务占比
Context Pollution Rate = 无关/陈旧信息被错误采纳的样本占比
```

#### M29 自我评估准确率（Self-Evaluation Accuracy / 校准）
```
Calibration = 对自身输出正确性判断与实际一致的样本占比
```
- 关键：判断 Agent “知道自己错了没有”。过低 = 盲目自信；过高 = 该停不停、过度改。

---

### 3.7 D7 用户体验（User Experience）

#### M30 用户满意度（CSAT / NPS）
```
CSAT = 满意及以上评分用户占比（1–5 分中 ≥4）
NPS  = 推荐者占比 - 贬损者占比（0–10 分）
```

#### M31 首次解决率（First Contact Resolution, FCR）
```
FCR = 单轮/单会话内解决问题且用户无后续追问或投诉的占比
```

#### M32 放弃率 / 转人工率
```
Abandonment Rate = 会话中途退出占比
Escalation Rate  = 需要转人工/专家介入的占比
```

---

## 4. 综合评分模型

### 4.1 北极星指标（建议）

```
任务有效完成率 = 任务成功率 × 一次性通过率
```
或更平衡的复合：
```
综合质量分 = w1·TSR + w2·正确率 + w3·(1-幻觉率) + w4·成本效率分 + w5·安全性分
```
（权重示例：`0.30 / 0.25 / 0.15 / 0.15 / 0.15`，须按业务裁剪。）

### 4.2 归一化
- 比率类：天然 `[0,1]`。
- 时延/成本/步数：用**对数或分位数归一化**，或相对基线（Baseline 模型）取比值：
```
效率分 = Baseline 成本 / 当前成本  （上限截断到 1）
```

### 4.3 加权聚合
```
维度分 D_j = Σ_i (w_ij · M_ij)          # 维度内指标加权
总分      = Σ_j (W_j · D_j)             # 维度间加权
```
- 权重必须显式声明、可配置，且随业务阶段调整（冷启动期重安全，成熟期重效率）。

### 4.4 一票否决项（Hurdle / Gating）
以下指标不参与加权，而是作为**硬性门槛**：
- 有害内容率 > 阈值 → 直接判不合格，禁止上线。
- PII 泄露率 > 0（或阈值）→ 直接判不合格。
- 越权率 > 0 → 直接判不合格。

---

## 5. 评估方法（如何测出这些指标）

### 5.1 三通道判定机制（结果判定）
| 通道 | 适用 | 特点 |
|---|---|---|
| **可执行断言**（单元测试式） | 有确定答案/可验证副作用的任务 | 客观、可复现、零成本 |
| **LLM-as-judge** | 开放式/软性任务 | 需 rubric + 位置偏置控制 + 多次采样 |
| **人工抽检** | 高价值、争议样本、校准 judge | 最权威、最贵，用于兜底与校准 |

### 5.2 LLM-as-judge 规范（防自评偏差）
1. 用**与被测 Agent 不同的模型**作 judge，或至少不同配置/系统提示。
2. 提供**分维度 rubric**（每档有锚定示例），而非笼统“打分”。
3. 控制**位置偏置**（交换候选顺序跑两次取均值）。
4. 报告 **judge 与人工的一致性**（Cohen's κ / 一致率），κ < 0.6 需重训 judge。

### 5.3 轨迹评估（Trace Evaluation）
- 采集每个 Agent 运行的结构化轨迹：`[step, reasoning, tool, params, result, tokens, latency]`。
- 用规则引擎自动计算 D6 全部指标 + D3 成本指标。
- 对关键轨迹做**定性复盘**（Why/What Went Wrong），产出问题归因。

### 5.4 离线评测集 + 在线监控 双轨
| 阶段 | 手段 | 目的 |
|---|---|---|
| 离线（发版前） | 静态评测集 + 回归 | 防退化、选模型、调 Prompt |
| 在线（发版后） | 采样监控 + A/B + 用户反馈 | 看真实分布、看长尾 |

### 5.5 A/B 实验
- 对比新旧版本/模型的**北极星指标**与分维度指标，报告置信区间（p 值），样本量需达统计显著。

---

## 6. 评测集设计规范

### 6.1 任务分类法（必须全覆盖）
| 类别 | 示例（知识库场景） | 关键指标 |
|---|---|---|
| 事实问答 | “XX 产品的保修期是多久？” | 正确率、引用准确率、幻觉率 |
| 多跳推理 | “A 和 B 两个政策的差异？” | 召回率、忠实度、步骤效率 |
| 检索综合 | “汇总近三月的相关政策” | 召回@k、NDCG、完整性 |
| 工具调用 | “把这份文档转成 PDF 并归档” | 工具成功率、参数正确率 |
| 开放生成 | “写一份培训手册” | 生成质量分、指令遵循率 |
| 拒答/边界 | 越权、超范围、无答案 | 拒答准确率 |
| 对抗/安全 | 注入攻击、诱导越权 | 有害率、越权率、鲁棒性 |

### 6.2 难度分级
- L1 简单（单跳、直接命中）、L2 中等（多跳/需综合）、L3 困难（含干扰、矛盾、长文档）、L4 对抗（含陷阱）。
- 按难度分别报告指标，避免“用简单题刷分”。

### 6.3 质量要求
- 每个任务有**黄金参考答案**或**可验证断言**；软性任务必须有**评分 rubric**。
- 评测集需要版本化管理、去重、去污染（与训练数据隔离）。
- 规模建议：核心集 ≥200 条，回归集 ≥50 条，抽样足够保证置信。

---

## 7. 落地实施路线

1. **埋点**：在 Agent 运行时输出结构化 trace（见 §5.3），这是所有指标的数据底座。
2. **搭建评测集**：先建 50–100 条金标核心集。
3. **实现自动打分**：断言通道 + LLM-as-judge 通道 + trace 规则引擎。
4. **建立基线**：对当前版本跑一遍，产出基线报告。
5. **接入 CI/发版门禁**：核心集回归 + 一票否决项门禁。
6. **在线监控看板**：实时展示 D1/D3/D4/D5 在线指标，设置告警阈值。
7. **定期人工校准**：每月抽检 judge 一致性，更新评测集。

---

## 8. 指标速查表（附录）

| 编号 | 指标 | 公式（简化） | 维度 | 数据来源 |
|---|---|---|---|---|
| M1 | 任务成功率 TSR | 成功数/总数 | D1 | 判定结果 |
| M2 | 目标达成率 GAR | 达成数/总数 | D1 | 世界状态校验 |
| M3 | 一次性通过率 FPR | 一次对数/总数 | D1 | 判定+人工 |
| M4 | 完成度 CR | Σ子要求完成比/N | D1 | 判定 |
| M5 | 正确率 | 正确数/总数 | D2 | 判定 |
| M6 | 精确率/召回率/F1 | TP/FP/FN | D2 | 判定 |
| M7 | 幻觉率 | 含幻觉数/总数 | D2 | judge+人工 |
| M8 | 引用准确率 | 正确引用/总引用 | D2 | judge+人工 |
| M9 | 检索相关度 | Hit@k/MRR/NDCG | D2 | 检索日志 |
| M10 | 生成质量分 | judge 1–5 | D2 | judge |
| M11 | 端到端时延 | p50/p90/p99 | D3 | 运行时 |
| M12 | Token 成本 | 输入+输出 | D3 | 运行时 |
| M13 | 步骤效率 | 步数 | D3 | trace |
| M14 | 工具调用效率 | 调用次数 | D3 | trace |
| M15 | 单成功成本 | 成本/成功数 | D3 | 运行时 |
| M16 | 一致性 | 多次运行一致占比 | D4 | 多次运行 |
| M17 | 鲁棒性 | 扰动后成功率比值 | D4 | 扰动测试 |
| M18 | 错误恢复率 | 出错后仍成功占比 | D4 | trace |
| M19 | 可用性 | 无异常会话占比 | D4 | 运行时 |
| M20 | 有害内容率 | 有害数/总数 | D5 | judge+安全扫描 |
| M21 | 拒答准确率 | P/R | D5 | judge |
| M22 | 越权率 | 越权数/总数 | D5 | 权限审计 |
| M23 | PII/提示词泄露率 | 泄露数/总数 | D5 | 扫描+judge |
| M24 | 指令遵循率 | 满足约束数/总数 | D5 | judge |
| M25 | 工具调用成功率 | 成功调用/总调用 | D6 | trace |
| M26 | 规划准确率 | 规划合理数/总数 | D6 | trace+judge |
| M27 | 终止合理性 | 异常终止占比 | D6 | trace |
| M28 | 上下文管理质量 | 约束保持率 | D6 | trace |
| M29 | 自我评估校准 | 判断一致占比 | D6 | trace+判定 |
| M30 | CSAT/NPS | 满意度占比 | D7 | 用户反馈 |
| M31 | 首次解决率 FCR | 一次解决占比 | D7 | 会话日志 |
| M32 | 放弃/转人工率 | 退出/转接占比 | D7 | 会话日志 |

---

## 9. 知识库/RAG Agent 专项建议

结合本项目（PDH-PKG），建议**优先落地**以下最小可用指标集（MVP，先跑起来）：

1. **任务成功率（M1）** + **正确率（M5）**：衡量整体。
2. **幻觉率（M7）** + **引用准确率（M8）**：PDH-PKG 对“不可编造、可溯源”要求最高。
3. **检索相关度（M9，Hit@5/MRR）**：隔离定位检索问题。
4. **端到端时延（M11）** + **Token 成本（M12）**：控制生产成本。
5. **越权率（M22）** + **PII 泄露率（M23）**：企业数据合规的硬门槛。
6. **工具调用成功率（M25）**：知识库 Agent 常需调用检索/重排/改写工具。

> **已落地**（`agent_eval/` 工具包，说明见 `agent_eval/README.md`）：
> - ✅ 结构化 trace 格式：`agent_eval/schemas/trace.schema.json` + `agent_eval/models.py: Trace`
> - ✅ 评测集 JSON Schema：`agent_eval/schemas/evalset.schema.json` + 示例 `data/evalsets/example_evalset.json`
> - ✅ judge rubric 模板：`agent_eval/rubrics.py`（断言 + LLM 裁判 + PII/泄露扫描三通道见 `agent_eval/judge.py`）
> - ✅ 指标计算脚本：`agent_eval/metrics.py`（M1–M32 离线可算部分 + 综合评分 + 一票否决）+ 报告/看板 `agent_eval/report.py`
> - ✅ 评估流水线与 CLI：`python -m agent_eval run|report`（`agent_eval/runner.py`、`cli.py`）
> - ✅ 单元测试：`tests/test_agent_eval.py`
>
> 尚未覆盖（需在线数据/人工）：M6、M26、M28–M32 —— 计算报告会列明数据源缺口。

---

## 10. 参考基准与数据集（联网调研）

设计本体系与评测集时参考的公开基准/数据集（形式与方法参考，不直接照搬内容）：

| 基准/数据集 | 关注点 | 对我们的借鉴 |
|---|---|---|
| [UHGEval 幻觉评估数据集](https://hub.baai.ac.cn/view/33685) | 中文 RAG 幻觉细粒度评估 | 幻觉分型（事实/引用/归因）与逐条标注做法 |
| [Chinese SimpleQA（ACL 2025）](https://papernotes.org/ACL2025/llm_safety/chinese_simpleqa_a_chinese_factuality_evaluation_for_large_language_models/) | 中文事实性评测 | 短答案 + 可判定参考答案/别名设计 |
| [CRAG Benchmark](https://opea-project.github.io/1.3/GenAIEval/evals/evaluation/agent_eval/crag_eval/README.html) | RAG Agent QA 端到端评测 | 任务分类、难易分层、检索与生成分开打分 |
| [AgenticRAGTracer](https://huggingface.co/datasets/YqjMartin/AgenticRAGTracer) | Agentic RAG 轨迹级标注 | 轨迹字段（工具、步骤、引用）设计参考 |
| [Plancraft](https://www.research.ed.ac.uk/en/publications/plancraft-an-evaluation-dataset-for-planning-with-llm-agents/) | LLM Agent 规划评测 | 规划合理性判定（对应 M26） |
| [NAACL 2025: RAG 部署验证](https://aclanthology.org/2025.naacl-industry.53/) | 企业级 RAG 部署验证 | 上线门禁指标行业做法 |

> 金标答案一律以 `data/documents/` 内实际文档为准，不照搬外部数据集内容。
